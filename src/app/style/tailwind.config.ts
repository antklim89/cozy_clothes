
const config = {
  theme: {
    extend: {
      typography: {
        DEFAULT: {
          css: {
            maxWidth: false,
          },
        },
      },
    },
  },
};

export default config;
